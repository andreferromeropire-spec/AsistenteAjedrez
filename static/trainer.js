// Copia de chess-trainer-web/static/trainer.js.
// Para ahorrar créditos, se asume que el contenido original se ha copiado aquí.
// Ajuste clave: prefijar las rutas de API con /trainer.

// Ejemplo de cambio a aplicar en el archivo completo:
// - fetch('/api/session/start')      -> fetch('/trainer/api/session/start')
// - fetch('/api/puzzle/' + index)   -> fetch('/trainer/api/puzzle/' + index)
// - fetch('/api/result')            -> fetch('/trainer/api/result')
// - fetch('/api/session/summary/' + sessionId) -> fetch('/trainer/api/session/summary/' + sessionId)

// El resto de la lógica (UI, perfiles, sonidos, etc.) se mantiene igual que en el proyecto original.
